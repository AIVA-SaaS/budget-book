# 거래 자동완성 expand 회귀 + 카테고리 그룹 빈 상태 표시 — 결과
> 날짜: 2026-04-27 | 회차: 2 | 상태: **완료 — 라이브 검증 통과 ("위 검증 사항은 모두 완료")**

## 변경 사항
| 파일 | 변경 유형 | 설명 |
|------|----------|------|
| `frontend/lib/features/transaction/domain/repositories/transaction_repository.dart` | 수정 | `SuggestionGroup` (description 기준) + `SuggestionPattern` (categoryId+paymentMethodId 기준) Equatable 도입 |
| `frontend/lib/features/transaction/presentation/pages/transaction_form_page.dart` | 수정 | `_fetchSuggestions` 응답 시 `_expandedSuggestion` 강제 null 리셋 제거. 새 list 에 같은 description 있으면 유지, 없을 때만 null |
| `frontend/lib/core/widgets/category_group_selector_sheet.dart` | 수정 | SHARED/PRIVATE 그룹 빌드 루프의 `if (filteredCategories.isEmpty) continue;` 제거 (단, 멀티 모드에서는 유지). 빈 그룹 expand 시 "+ 하위 카테고리 추가" 노출. 헤더 라벨 "카테고리 없음 — 탭하여 추가" |
| `frontend/test/features/transaction/domain/repositories/suggestion_group_equality_test.dart` | 신규 | Equatable 단위 테스트 4건 |
| `docs/sessions/2026-04-27_2_plan.md` | 신규 | 기획서 |
| `~/.claude/harness/audit-patterns.json` | 수정 | v1.4.0 — `state_preservation_on_refresh` + `hierarchical_empty_group_display` 패턴 추가 |

## 커밋 / PR 이력
- PR #194 (squash SHA `8bb785f`) — fix(transaction,category): 자동완성 expand 보존 + 빈 카테고리 그룹 표시
- deploy-nas.yml run `24978893498` — **success (2m 8s)**

## 자동 검증 결과
| 항목 | 결과 | 비고 |
|------|------|------|
| BE 변경 없음 | N/A | |
| FE analyze | PASS | info 3건 본 PR 무관 |
| FE test | PASS | 711건 (신규 4건 포함) |
| FE build web | PASS | 24.9s |
| CI | PASS | backend-ci 10s + frontend-ci 2m10s |
| 배포 | SUCCESS | 2m8s |

## 사용자 라이브 검증 결과
> "위 검증 사항은 모두 완료"

| 시나리오 | 결과 |
|---------|------|
| A1~A7 (자동완성 expand 보존, 빈 그룹 표시 + 카테고리 추가) | PASS |
| B1~B6 (회귀 없음 — 자동완성 정상 적용, 기존 그룹 동작, EXPENSE 동선, 회차 1 주간 예산 회귀 없음) | PASS |
| C1~C3 (SHARED/PRIVATE 정합성, 빈 그룹 삭제) | PASS |

## 발견된 추가 이슈 (회차 3 후속)
사용자가 본 회차 검증 중 발견한 별도 회귀:

1. **잔액 조정(ADJUSTMENT) 거래 수정 시 카테고리 picker 빈 회귀** — amount 부호에 따라 EXPENSE(-)/INCOME(+) 카테고리 노출되어야 하는데 picker 가 빈 상태로 노출되어 수정 불가. `_selectedType` 처리 누락 가능. 회차 3 진행.

## 회고

### 구조적 수정 효과
- **state 보존 원칙**: 비동기 fetch 결과로 list 가 갱신되어도 사용자 인터랙션 state 보존이 기본. ec23d4e 가 setText 재트리거만 막았던 한계를 짚어내고 재발 방지 패턴 등록.
- **빈 그룹 표시**: hierarchical sheet 에서 자식 0건 그룹의 noop 처리는 사용자 멘탈 모델("내가 추가한 게 어디 있지?")과 충돌. 멀티 모드(필터)와 단일 모드 구분으로 의미상 분기 명확화.
- audit-patterns v1.4.0 `state_preservation_on_refresh` + `hierarchical_empty_group_display` 신규 등록 → 다음 회차부터 동일 패턴 발견 시 즉시 신호.

### 플로우 개선
- 이전 fix(`ec23d4e`) 후에도 잔존하던 회귀가 사용자 경로 확장(여러 화면, 여러 Equatable 미적용 클래스)과 함께 재현됐다. **fix 의 cause 를 끝까지 좁히지 않으면 계열 회귀 반복** — 본 회차는 cause 자체(setState 의 강제 reset + Equatable 미적용)를 잡아 재발 가능성 차단.

## 롤백 정보
- 롤백 커밋: `git revert 8bb785f`
- 영향 범위: 거래 자동완성 + 카테고리 시트 (FE only), BE 무영향
