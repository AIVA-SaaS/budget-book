# 거래 표시 통일 — 결제수단 표시 + 홈/거래 탭 공통 위젯화
> 날짜: 2026-03-25 | 회차: 1 | 상태: 기획

## 요청 내용
1. 거래 목록에서 결제수단이 표시되어야 함 (아이콘과 거래내용 사이)
2. 홈 탭 최근 거래와 거래 탭 목록이 동일한 위젯을 사용해야 함 (현재 중복 코드)

## 영향 범위 분석
- **변경 파일**:
  - `frontend/lib/features/transaction/presentation/widgets/transaction_list_tile.dart` — 결제수단 표시 추가
  - `frontend/lib/features/home/presentation/pages/dashboard_page.dart` — 인라인 거래 UI를 `TransactionListTile` 위젯으로 교체
- **관련 기존 코드**:
  - `TransactionListTile`: 거래 탭에서만 사용 중 (leading: CircleAvatar, subtitle: 카테고리+포켓)
  - `dashboard_page.dart:670-740`: 홈 탭 최근 거래를 인라인 Row로 직접 그림 (중복)
- **기술적 제약**: 홈 탭에서는 스와이프 삭제 불필요 (onDelete=null), onTap은 거래 탭 이동
- **이전 세션**: develop에서 비슷한 작업을 했으나 규칙 미준수로 stash됨

## 현재 상태 비교

### 거래 탭 (TransactionListTile)
```
[카테고리 아이콘]  거래내용              -12,000원
                   식비 | 포켓태그       작성자
```

### 홈 탭 (인라인 코드 — 중복)
```
[↓ 아이콘]  거래내용              -12,000원
            2026-03-25  식비
```
- 다른 아이콘 (카테고리 vs 수입/지출 화살표)
- 다른 subtitle (카테고리+포켓 vs 날짜+카테고리)
- 결제수단 미표시

## 작업 계획

### 작업 1: TransactionListTile에 결제수단 표시 추가
- leading 영역에 카테고리 아이콘 아래 결제수단명 소형 텍스트
- 레이아웃: `Column(아이콘, 결제수단명)`

```
[🍔 아이콘 ]  거래내용              -12,000원
 삼성카드     식비 | 포켓           작성자
```

### 작업 2: 홈 탭 최근 거래를 TransactionListTile로 교체
- `dashboard_page.dart:670-740`의 인라인 코드 삭제
- `TransactionListTile(transaction: txn, onTap: ...)` 사용
- `onDelete` 미전달 (홈에서는 스와이프 삭제 불필요)

## 성능 설계
- 위젯 재사용으로 코드 감소 (인라인 ~70줄 → 위젯 1줄)
- 렌더링 성능 동일 (ListTile 기반)
- 추가 API 호출 없음

## 검증 계획
- [ ] `flutter analyze` 통과
- [ ] `flutter test` 통과
- [ ] `flutter build web` 성공
- [ ] 유저 플로우 검증:
  - [ ] 거래 탭: 결제수단이 아이콘 아래에 표시되는가?
  - [ ] 거래 탭: 결제수단 없는 거래도 정상 표시되는가?
  - [ ] 홈 탭: 최근 거래가 거래 탭과 동일한 UI로 표시되는가?
  - [ ] 홈 탭: 거래 탭하면 거래 상세/목록으로 이동하는가?
  - [ ] 홈 탭: 스와이프 삭제가 비활성화되어 있는가?

## 사용자 승인
- [ ] 승인 / 수정 요청: ___
