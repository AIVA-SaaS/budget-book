# 합계 일관성 구조 fix + LoadTransactions 필터 보존 — 결과
> 날짜: 2026-04-28 | 회차: 8+9 통합 | 상태: **완료 — 라이브 검증 통과 ("모두 유지된다")**

본 result 는 회차 8 (BE summary 모든 필터 + FE client fold 제거) + 회차 9 (LoadTransactions 호출부 필터 보존) 묶음. 회차 8 라이브 검증 중 발견된 새 회귀 (탭 전환 시 필터 reset) 를 회차 9 로 즉시 fix.

## 변경 사항

### 회차 8 (PR #206, SHA `207659c`)
| 파일 | 변경 |
|------|------|
| `backend/src/main/kotlin/com/budgetbook/statistics/service/StatisticsService.kt` | `getMonthlySummary` 시그니처 확장 — categoryId, paymentMethodId, pocketId, *Ids, transactionTypes, amountMin/Max, keyword 추가. 필터 활성 시 `TransactionSpecifications.withFilters` 사용 (period-summary 패턴) |
| `backend/.../StatisticsController.kt` | `/summary` `CommonFilterParams` 의 모든 필드 service 전달 |
| `frontend/lib/features/statistics/domain/repositories/statistics_repository.dart` | `getSummary` 시그니처 확장 |
| `frontend/lib/features/statistics/data/repositories/statistics_repository_impl.dart` | impl 갱신 |
| `frontend/lib/features/statistics/data/datasources/statistics_remote_datasource.dart` | datasource + query params 모든 필터 전달 |
| `frontend/lib/features/transaction/presentation/bloc/transaction_bloc.dart` | `hasNoFilters` / `hasOnlyPaymentMethodFilter` 분기 제거. 항상 BE summary 호출 + 모든 필터 전달 |
| `frontend/lib/features/transaction/presentation/pages/transaction_list_page.dart` | `hasClientFilter` 분기 제거. server total 미수신 fallback 만 유지 |
| `~/.claude/harness/audit-patterns.json` | v1.9.0 `be_summary_filter_passthrough` 등록 |
| `docs/sessions/2026-04-28_1_plan.md` | 기획서 |

### 회차 9 (PR #207, SHA `d713b5c`)
| 파일 | 변경 |
|------|------|
| `frontend/lib/core/widgets/main_shell_page.dart` | `_onDestinationSelected` case 0 — `currentFilter` 의 16개 필드 전달 |
| `frontend/lib/core/websocket/sync_event_handler.dart` | `_refreshTransactions` — `currentFilter` 보존 |
| `frontend/lib/core/router/app_router.dart` | 거래 탭 builder 의 `LoadTransactions` — URL explicit param 우선 + `currentFilter` fallback |
| `~/.claude/harness/audit-patterns.json` | `transaction_load_filter_preservation` 신규 |

## 커밋 / PR 이력
- PR #206 (squash `207659c`) — fix(statistics): 합계 일관성 — BE getMonthlySummary 모든 필터 + FE client fold 제거
- deploy run `25035692786` — success
- PR #207 (squash `d713b5c`) — fix(transaction): 탭 전환/sync 시 LoadTransactions 필터 보존
- deploy run `25037945050` — success

## 자동 검증 결과
| 항목 | 결과 |
|------|------|
| BE statistics 테스트 | PASS |
| FE analyze | PASS |
| FE test | PASS (717건) |
| FE build web | PASS |
| CI (2 PR) | PASS |
| 배포 (2 PR) | SUCCESS |

## 사용자 라이브 검증 결과
- 회차 8 합계 정확성: visibility/카테고리/결제수단/조합 필터 → BE 정확 계산 → PASS
- 회차 8 회귀 발견: 거래 → 분석 → 거래 복귀 시 chip vs list 불일치
- 회차 9 fix 후: **"모두 유지된다"** — 탭 전환 / WebSocket sync / 라우트 진입 모두 필터 보존 → PASS

## 회고

### 회차 8 의 한계와 회차 9
회차 8 은 BE summary 정확성에 집중했으나 **LoadTransactions 호출부의 필터 누락 케이스를 놓침**. main_shell_page / sync_event_handler / app_router 의 3곳에서 `LoadTransactions(year, month)` 단수 호출이 잔존. 사용자 라이브 검증으로 발견 → 회차 9 즉시 fix.

이는 audit-patterns 의 한계도 노출: v1.9.0 의 `be_summary_filter_passthrough` 가 BE summary 호출만 점검. LoadTransactions 자체 호출부의 인자 누락은 별도 패턴 필요. 회차 9 에서 `transaction_load_filter_preservation` 신규 등록.

### 구조적 수정 효과
- 페이징 단위 client fold 부정확 회귀 근본 fix
- BE 측 단일 진입점 (`getMonthlySummary` 가 모든 필터 받음, period-summary 패턴 통일)
- FE 호출부 통일 (currentFilter value object 의 16 필드 spread)
- audit-patterns v1.9.0 에 두 신규 패턴 (BE 측 + FE 측) 등록

### Follow-up (회차 10 후보)
- **다른 statistics 엔드포인트** (`by-category`, `payment-methods`, `monthly-trend`) 모든 필터 지원 — 회차 8 범위 밖. 통계 페이지의 합계도 일관성 필요.
- **dashboard / budget summary** 의 client fold 점검 — 비슷한 패턴 잔존 가능.
- **다른 Load 이벤트** (LoadBudgets, LoadStatistics, LoadDashboard 등) 의 필터 보존 점검.
- **BottomNav 분석 영역 재시도** (회차 7 revert 후 follow-up).

## 롤백 정보
- PR #206 롤백: `git revert 207659c` (BE+FE 모두 영향, 큰 범위)
- PR #207 롤백: `git revert d713b5c` (FE only)
