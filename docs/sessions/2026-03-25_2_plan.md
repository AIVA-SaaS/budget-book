# 예산 알림 그룹명 표시 수정
> 날짜: 2026-03-25 | 회차: 2 | 상태: 기획

## 요청 내용
1. 예산 알림에서 그룹 예산이 "전체"로 표시됨 → 그룹명(예: "생활비")으로 표시
2. 주간 예산의 월 일할 계산 금액이 정상 적용되었는지 확인

## 영향 범위 분석
- **변경 파일**: `frontend/lib/features/budget/presentation/widgets/budget_alert_widget.dart`
- **관련 코드**:
  - Line 88: `item.category?.name ?? '전체'` → `item.category?.name ?? item.groupName ?? '전체'`
  - BE `BudgetSummaryItemResponse`는 이미 `groupId`, `groupName` 반환 중 (확인됨)
  - FE `BudgetSummaryItem` 엔티티에 이미 `groupId`, `groupName` 필드 있음 (확인됨)
- **근본 원인**: FE 알림 위젯이 `groupName` 필드를 확인하지 않음 (코드 누락)
- **주간 예산 금액**: BE `budget.amount`에 월환산 금액 저장 → summary API도 월환산 반환. 정상으로 판단되나 실데이터 확인 필요

## 작업 계획
| # | 작업 | 파일 | 난이도 |
|---|------|------|--------|
| 1 | 알림 위젯 groupName 표시 | budget_alert_widget.dart | S |

## 검증 계획
- [ ] `flutter analyze` 통과
- [ ] `flutter build web` 성공
- [ ] 유저 플로우: 그룹 예산(생활비) 알림에서 "생활비"로 표시되는가?
- [ ] 유저 플로우: 카테고리 예산 알림은 기존대로 카테고리명 표시되는가?
- [ ] 유저 플로우: 전체 예산(카테고리/그룹 없음)은 "전체"로 표시되는가?

## 사용자 승인
- [ ] 승인 / 수정 요청: ___
