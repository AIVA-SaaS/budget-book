# Phase 22 통합 검토 + 후속 계획
> 날짜: 2026-04-22 | 회차: 2 | 상태: **검증완료 / 진행 지시 대기**

## 0. 이 문서의 목적
Phase 22(이체/결제/ADJUSTMENT/필터 UI) 로 머지된 8개 PR 을 되짚어 **중복·회귀·누락**을 진단하고, 남은 작업을 **단일 우선순위 큐**로 정리한다. 하네스 TaskCreate 로 반영.

## 1. 머지된 PR 분석

| # | PR | 역할 | 상태 | 비고 |
|---|----|------|------|------|
| 1 | #119 `docs` | 기획서 | ✅ | §2.1–§10 확정본 |
| 2 | #120 PR-A | FE 핫픽스 — BANK→CREDIT 휴리스틱 지출 제외 | ✅ | **PR-C 가 superseded** (아래 §2.1) |
| 3 | #121 PR-B | BE: TransferKind 4값 + ADJUSTMENT + V54 + ExpenseCalculator | ✅ | 798/798 tests |
| 4 | #122 PR-C | FE: TransferKind 파싱 + LedgerSummary + 이체 종류 UI + 잔액 수정 UI + 다중 필터 | ✅ | 699/699 tests, rebase 2회 |
| 5 | #123 | Deploy verify 재시도 루프 (15×2s) | ✅ | #125 에서 삭제됨 |
| 6 | #124 | deploy-backend wait 5→10회 (60s), verify 20×3s | ✅ | |
| 7 | #125 | verify job 통째 제거 | ✅ | 중복 제거, 이슈 스팸 차단 |
| 8 | #126 | SelectableChipGroup 공용 위젯 + 전체 칩 | ✅ | 707/707 tests |

## 2. 중복·회귀·누락 진단

### 2.1 PR-A ↔ PR-C 중복 해소 확인 ✅
- **PR-A** (line 450–475) 에 `isCardSettlement = src.type=='BANK' && dst.type=='CREDIT'` 휴리스틱 추가
- **PR-C rebase** 시 같은 영역이 `LedgerSummary.from()` 으로 전량 교체 (commit `a765566`)
- 최종 main 에는 PR-A 코드 없음 (git blame 확인). **역할은 LedgerSummary 가 흡수** — CARD_SETTLEMENT kind 기준 제외.
- 결론: PR-A 과도기적 hotfix 는 정상적으로 제거됨, 이중 로직 없음.

### 2.2 BE 이중 쓰기 안전장치 (의도된 과도기) ⚠️
```kotlin
// TransferService.kt
isCardSettlement = resolvedKind == TransferKind.CARD_SETTLEMENT  // ← sync
kind             = resolvedKind                                    // ← 신규
```
- V55 에서 `is_card_settlement` 컬럼 DROP 할 때 **함께** 제거해야 함
- 현재 코드는 의도된 2중 기입 — 중간 분기 상태 방지

### 2.3 Deprecated API 잔존 ⚠️
| 위치 | 항목 | 제거 시점 |
|---|---|---|
| `TransferRepository.kt:139,163` | `sumAmountBy{Source,Destination}ExcludingSettlement` @Deprecated | V55 |
| `Transfer.kt:74-75` | `var isCardSettlement: Boolean` 컬럼+속성 | V55 |
| 테스트 4건 | 위 메서드 mocking | V55 |
| `transfer_model.dart:38-40` | FE legacy `isCardSettlement` JSON fallback | V55 BE 배포 후 |
| `transfer.dart:77` | `bool get isCardSettlement` derived getter | 계속 유지 가능 (호출부 많음) — 선택 |
| `filter_chip_group.dart` | `FilterChipGroup` 단일 전용 위젯 | spending_plan 호출부(1건) 치환 후 |

### 2.4 누락·미작성 📋
- `docs/CODEMAPS/TRANSFER_SYSTEM.md` 미작성 (기획서 §9 약속)
- `docs/api-spec.md` / OpenAPI 업데이트 미반영 (kind, totalTransfer, ADJUSTMENT, transactionTypes)
- `docs/sessions/2026-04-21_1_result.md` 미작성 (CLAUDE.md §3.3 권장)
- Issue #110 [DEPLOY FAIL] 2026-04-20 잔존 — 삭제된 verify job 이 생성한 stale 이슈

### 2.5 회귀 위험 없음 확인 ✅
- V54 Flyway: 완료 상태 (`schema v54`, 재실행 없음, idempotent 불필요)
- `ck_transactions_amount` 부호 허용 변경 — 기존 EXPENSE/INCOME 부호 규칙 유지
- `ExpenseCalculator` 도입 후 모든 집계 경로가 이 유틸만 사용 (S1 통과)
- `LedgerSummary.from` 이 FE 집계 유일 경로 (S2 통과)

## 3. 통합 우선순위 큐

### P0 — 즉시 (블로킹·잔일)
| ID | 작업 | 근거 |
|----|-----|------|
| **T1** | 라이브 검증 6항목 (§4) | 배포가 실제로 Phase 22 기능을 보여주는지 확인. 이것 없이는 V55 착수 불가 |
| **T2** | Issue #110 close | stale, 원인(verify job) 제거됨. 1분 작업 |

### P1 — 라이브 검증 완료 후
| ID | 작업 | 규모 |
|----|-----|------|
| **T3** | **V55 DROP 마이그레이션 + Deprecated 정리 PR** | M |

T3 상세 (단일 PR 권장 — 상호 의존 강함):
- `V55__drop_is_card_settlement.sql`
  ```sql
  ALTER TABLE transfers DROP COLUMN is_card_settlement;
  ```
- `Transfer.kt`: `isCardSettlement` 프로퍼티/컬럼 제거
- `TransferService.kt`: sync 쓰기 3곳 제거 (createTransfer/updateTransfer/createCardSettlement)
- `TransferRepository.kt`: `sumAmountBy...ExcludingSettlement` 메서드 2건 삭제
- 테스트 4건 업데이트 (Repo mock 제거)
- 주석 정리 (`Transaction.kt:64`, `TransferController.kt:43`)

### P2 — 독립, 병렬 가능
| ID | 작업 | 규모 |
|----|-----|------|
| **T4** | `(couple_id, transfer_date, kind)` 부분 인덱스 추가 (V56) | S |
| **T5** | `docs/CODEMAPS/TRANSFER_SYSTEM.md` 작성 (kind 매트릭스) | S |
| **T6** | OpenAPI / `docs/api-spec.md` 갱신 | S |
| **T7** | `filter_chip_group.dart` 제거 + spending_plan 호출부 `SelectableChipGroup.single` 치환 | S |
| **T8** | FE `transfer.dart:77` `isCardSettlement` derived getter 유지 여부 결정 (호출부 감사 후) | S |

### P3 — 선택
| ID | 작업 | 규모 |
|----|-----|------|
| **T9** | `docs/sessions/2026-04-21_1_result.md` 작성 (결과 기록 템플릿) | S |

## 4. 라이브 검증 체크리스트 (T1)

배포된 main (`b5476a7`) 기준.

- [ ] **V54 적용 확인** — BE 로그 `schema "public": 54` 가 현재 버전
- [ ] 기존 BANK→CREDIT 이체의 kind 가 `CARD_SETTLEMENT` 로 백필됨 (DB 조회)
- [ ] 거래 목록 필터 활성 + 카드 결제 존재 월: **지출 이중 계산 없음** (원본 EXPENSE 1회만)
- [ ] MonthSummaryBar **4칸**(수입/지출/이체/합계) 표시
- [ ] 이체 폼에서 "종류" 드롭다운 노출 + 방향 기반 자동 추천
- [ ] 결제수단 타일 popup 에서 "잔액 수정" → ADJUSTMENT 거래 생성 + 잔액만 변동 (통계 무반영)
- [ ] 거래 유형 필터 **"전체"** 칩 토글 정상 + 개별 3개 선택 시 "전체" 자동 on
- [ ] 공개 범위 칩 스타일 (`전체 | 공유 | 개인`) + 단일 선택 동작
- [ ] 기존 `FilterChipGroup` 사용처(`spending_plan_list_page.dart`) 회귀 없음

## 5. 성능 설계 재확인
- V54 백필 완료 상태 → 런타임 영향 없음
- ExpenseCalculator 쿼리 2건 (tx EXPENSE/INCOME sum, transfer by kind sum) — 기존 대비 +0~1 쿼리
- LedgerSummary.from O(N) 단일 패스 — N=수백 수준, 수 ms
- T4 인덱스 추가 시 kind filter selectivity 개선 — 현재는 row 수 낮아 무영향

## 6. 하네스 Scope Audit
- 이 문서는 분석·계획용 문서. 코드 변경 없음 → audit 대상 아님
- 실제 T3 PR 착수 시 `db_schema, amount_calculation` 태그로 재실행 필요
- T4 인덱스는 `db_schema` 태그로 재실행 필요

## 7. 결론 — 권장 처리 순서

1. **T1 (라이브 검증)** — 사용자 수동. 실패 항목 있으면 별도 패치 PR.
2. **T2 (Issue #110 close)** — 즉시 처리 가능.
3. 검증 통과 후 **T3 (V55 + 정리)** 단일 PR 로 일괄 진행.
4. **T4~T7** 는 독립이라 T3 와 병렬 또는 후행 가능.
5. **T8, T9** 는 선택.

## 8. 승인 / 진행 지시
- [ ] T1 (라이브 검증 보고) → 사용자
- [ ] T2 (Issue #110 close) 즉시 진행
- [ ] T3 (V55 정리 PR) 검증 후 착수 — 진행 지시 시 시작
- [ ] T4–T7 병렬 진행 여부 결정
