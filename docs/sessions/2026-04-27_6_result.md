# 거래 sub-page BottomNav 유지 (1차) — 결과
> 날짜: 2026-04-27 | 회차: 6 | 상태: **완료 — 라이브 검증 통과 ("유지 잘된다")**

## 변경 사항
| 파일 | 변경 유형 | 설명 |
|------|----------|------|
| `frontend/lib/core/router/app_router.dart` | 수정 | `/transactions/{create, edit/:id, detail/:id, import}` 4개 라우트를 ShellRoute Tab 0 의 nested route 로 이전. 기존 root level GoRoute + `parentNavigatorKey: _rootNavigatorKey` 4개 라인 삭제. builder 본체 동일 |
| `docs/sessions/2026-04-27_6_plan.md` | 신규 | 기획서 |
| `~/.claude/harness/audit-patterns.json` | 수정 | v1.8.0 — `bottomnav_preservation` 패턴 등록 |

## 커밋 / PR 이력
- PR #202 (squash SHA `cf84d86`) — feat(router): 거래 sub-page BottomNav 유지 (1차) — Tab 0 nested
- deploy-nas.yml run `24986757437` — **success (2m 15s)**

## 자동 검증 결과
| 항목 | 결과 |
|------|------|
| FE analyze | PASS (info 3건 무관) |
| FE test | PASS (717건) |
| FE build web | PASS |
| CI | PASS |
| 배포 | SUCCESS |

## 사용자 라이브 검증 결과
> "유지 잘된다."

- A1~A7 (4 sub-page 진입 + 탭 전환 + stack 보존) → PASS
- B1~B4 (deep link, 새로고침, 뒤로가기, 회차 1~5 회귀 없음) → PASS

## 발견된 추가 이슈 (회차 7+ 후속)
- **나머지 ~38 sub-page** 도 동일 정책 적용 필요. 영역별로 묶어 단계적 이행:
  - 예산/분석 (Tab 1): `/budgets/{create,edit/:id}`, `/weekly-budgets`, `/weekly-budgets/settlement`, `/reports`, `/period-summary`, `/spending-plans/*`
  - 거래 부속 (Tab 0): `/transfers/*`, `/pocket-transfers`, `/card-settlement`
  - 자산/관리 (해당 탭 결정 필요): `/asset-management`, `/payment-methods`, `/category-groups`, `/pockets/*`
  - 정기/보험: `/recurring/*`, `/insurances/*`
  - 설정/기타: `/settings/*`, `/feedback*`, `/couple` 등

## 회고
- ShellRoute branch nesting 으로 BottomNav 유지가 단순 (path leading slash 만 변환). builder 본체는 그대로 옮김.
- audit-patterns `bottomnav_preservation` 가 신규 sub-page 추가 시 root navigator 사용 강제 차단 → 회귀 사전 방지.
- 단계적 이행: 영역별 PR 분리로 라이브 검증 부담 분산.

## 롤백 정보
- 롤백 커밋: `git revert cf84d86`
- 영향 범위: 거래 sub-page 라우터 (FE only), BE 무영향
