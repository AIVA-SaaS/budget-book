# 잔액 조정(ADJUSTMENT) 거래 수정 시 카테고리 picker fix — 결과
> 날짜: 2026-04-27 | 회차: 3 | 상태: **부분 완료 — picker 비활성 라벨 고정 PASS, 추가 요건은 회차 4/5 로 이행**

## 변경 사항
| 파일 | 변경 유형 | 설명 |
|------|----------|------|
| `frontend/lib/features/transaction/presentation/pages/transaction_form_page.dart` | 수정 | `_buildCategoryPicker` 에 ADJUSTMENT 분기 — read-only "잔액 조정" 라벨 + onTap 안내 SnackBar. `_onSubmit` 의 카테고리 필수 검증 ADJUSTMENT 시 skip |
| `frontend/test/features/transaction/presentation/pages/adjustment_category_picker_test.dart` | 신규 | ItemSelectorField stateless 단독 회귀 테스트 2건 |
| `docs/sessions/2026-04-27_3_plan.md` | 신규 | 기획서 |
| `~/.claude/harness/audit-patterns.json` | 수정 | v1.5.0 — `special_type_picker_branch` 패턴 추가 |

## 커밋 / PR 이력
- PR #196 (squash SHA `d8bc96f`) — fix(transaction): 잔액 조정(ADJUSTMENT) 거래 수정 시 카테고리 picker 고정
- deploy-nas.yml run `24980857642` — **success (2m 10s)**

## 자동 검증 결과
| 항목 | 결과 | 비고 |
|------|------|------|
| FE analyze | PASS | info 3건 본 PR 무관 |
| FE test | PASS | 713건 (신규 2건 포함) |
| FE build web | PASS | 25.8s |
| CI | PASS | |
| 배포 | SUCCESS | 2m10s |

## 사용자 라이브 검증 결과
> "나머지는 잘 된다."

| 시나리오 | 결과 |
|---------|------|
| A1~A6 (ADJUSTMENT 수정 시 카테고리 라벨 고정, amount 등 수정 후 저장, 통계 제외) | PASS |
| B1~B6 (회귀 없음 — EXPENSE/INCOME 동선, 회차 1/2 동작) | PASS |
| C1~C2 (데이터 정합성) | PASS |

## 발견된 추가 이슈 (회차 4/5 로 이행)
사용자가 본 회차 검증 후 추가 요건/회귀 보고:

1. **잔액 수정 카테고리 sheet 에 "잔액 수정" 만 노출 + 다른 카테고리/그룹 추가 차단** (회차 4)
2. **거래 추가(create) 시 ADJUSTMENT 만들 동선 없음** — Tab 0/1/2 만 있어서 잔액 조정 거래 등록 불가. 카테고리 sheet 에 "잔액 조정" 핀 카드 (PR-X3 sentinel) 복원 필요 (회차 4)
3. **하단 탭(BottomNavigationBar) sub-page 진입 시 사라짐** — `parentNavigatorKey: _rootNavigatorKey` 가 ~25개 라우트에 적용되어 ShellRoute 밖에서 push. UI 일관성 위해 기본 정책으로 BottomNav 유지 + 단계적 이행 (회차 5)

## 회고

### 단순 라벨 고정의 한계
- 회차 3 의 read-only 라벨 고정은 ADJUSTMENT 수정 시 picker 빈 상태 회귀를 즉시 막았으나, 사용자 의도 "잔액 수정만 노출 + 다른 추가 차단" 까지는 못 갔다.
- PR-X3 의 sentinel 카테고리 복원 + sheet 정책(`adjustmentOnly`/`showAdjustmentOption`) 까지 가야 거래 등록 동선까지 일관 — 회차 4 에서 처리.

### audit-patterns 효과
- v1.5.0 `special_type_picker_branch` 패턴은 향후 form 의 `_selectedType == 'INCOME' ? a : b` ternary 추가 시 ADJUSTMENT/TRANSFER 분기 검토를 강제. 본 회귀 같은 빈 picker 케이스 사전 차단.

## 롤백 정보
- 롤백 커밋: `git revert d8bc96f`
- 영향 범위: ADJUSTMENT 수정 화면의 카테고리 picker UX (FE only), BE/통계 무영향
