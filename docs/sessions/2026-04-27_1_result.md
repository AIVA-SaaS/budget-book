# 주간 예산 행 동선 통일 — 결과
> 날짜: 2026-04-27 | 회차: 1 | 상태: **완료 — 라이브 검증 통과 ("잘 된다")**

## 변경 사항
| 파일 | 변경 유형 | 설명 |
|------|----------|------|
| `frontend/lib/features/budget/presentation/widgets/budget_row_actions.dart` | 신규 | 공통 행 위젯 + `openEdit` / `menuButton` 정적 메서드. nullable required 시그니처로 categoryGroupId/dateFrom/To 누락 컴파일 가드 |
| `frontend/lib/features/budget/presentation/pages/budget_list_page.dart` | 수정 | 월간 `_buildBudgetTile` + 주간 `_buildCurrentWeekHero` 모두 BudgetRowActions 호출로 치환. 주간 hero 그룹/총 예산 행 활성화 + 주간 dateFrom/To 전달. 삭제 시 WeeklyBudgetBloc reload |
| `frontend/lib/features/weekly_budget/presentation/widgets/week_summary_card.dart` | 수정 | `onItemTap` 콜백을 `itemRowBuilder` (Widget Function(ctx, item, rowBody)) 로 교체. 카드 자체는 PopupMenu/InkWell 미보유 — 부모가 BudgetRowActions 주입 |
| `frontend/test/features/budget/presentation/widgets/budget_row_actions_test.dart` | 신규 | 위젯 테스트 5건 (row tap → push edit, menu render, group/total branch, delete confirmed/cancelled) |
| `docs/sessions/2026-04-27_1_plan.md` | 신규 | 기획서 |
| `~/.claude/harness/audit-patterns.json` | 수정 | v1.3.0 — `budget_row_action_unification` + `weekly_filter_propagation` 패턴 추가 |

## 커밋 / PR 이력
- PR #192 (squash SHA `a20853a`) — feat(budget): 주간 예산 행 동선 통일 — BudgetRowActions 공통 위젯
- deploy-nas.yml run `24977664135` — **success (1m 56s)**

## 자동 검증 결과
| 항목 | 결과 | 비고 |
|------|------|------|
| BE 변경 없음 | N/A | |
| FE analyze | PASS | info 3건 본 PR 무관 |
| FE test | PASS | 707건 (신규 5건 포함) |
| FE build web | PASS | 27.9s |
| CI | PASS | backend-ci 5s + frontend-ci 2m16s |
| 배포 | SUCCESS | 1m56s |

## 사용자 라이브 검증 결과
> "잘 된다."

| 시나리오 | 결과 |
|---------|------|
| A1~A9 (주간 hero/주차 카드 클릭=편집, 더보기=거래보기/수정/삭제, 카테고리/그룹/총) | PASS |
| B1~B5 (월간 모드 회귀, 주간 추가/달 변경/empty state) | PASS |
| C1~C3 (applyToFuture 동작, 삭제 정합성, dateFrom/To 필터) | PASS |

## 발견된 추가 이슈 (다음 회차 후속)
사용자가 본 회차 진행 중 별도 회귀를 4건 보고. 본 PR 와는 영역이 다르므로 다음 회차로 묶어 분석.

1. **거래 입력 자동완성 회귀** — 거래 등록 시 내용(memo) 입력 자동완성 항목이 클릭 시 사라졌다 다시 노출. 이전에 fix 했던 회귀 재현.
2. **거래 등록(수입) 카테고리 그룹 추가 회귀** — INCOME 거래 등록 시 카테고리 그룹 추가 동작 안 됨.
3. **수입 새 카테고리 그룹 거래 화면 미노출** — 수입 카테고리 그룹 추가했으나 거래 화면(목록/선택)에 안 보임. #2 와 같은 영역의 구체적 증상으로 추정.
4. **BE 배포 시간 회귀 (10~20분)** — 2026-04-26 E-2 이후 3-4분이던 deploy-nas.yml 가 산발적으로 10~20분으로 회귀. **본 PR(#192) deploy 는 1m56s 로 빠름** → cache miss / runner 변동이 원인일 가능성. 다음 회차에서 다른 시점 run 시간 분포 추적 필요.

## 회고

### 구조적 수정의 효과
하네스 audit 가 `STRUCTURAL_FIX_REQUIRED` 로 잠금 → 패치가 아닌 공통 위젯 추출 + nullable required 시그니처 + audit 패턴 갱신으로 대응. 결과:
- 행 동선 4 군데 인라인 → 1 위젯/메서드. 향후 행 종류가 추가되어도 `BudgetRowActions` 시그니처만 통과시키면 모든 동선 (편집/거래 보기/삭제) 자동 노출.
- categoryGroupId / dateFrom / dateTo 가 nullable required 라 호출부에서 명시적 null/value 결정 강제 → filter_propagation 회귀가 컴파일 단계에서 차단.
- audit-patterns v1.3.0 의 `budget_row_action_unification` + `weekly_filter_propagation` 가 다음 작업의 사전 audit 시 즉시 신호.

### 플로우 개선
- TEMPLATE/주간/월간 budget 의 모델적 동질성 (`MonthlyBudget.weeklyAmount` 가 주간 표현)을 사용자 경험 측면에서 단일화 — 향후 새 budget 표현 (예: 분기/연간) 추가 시 동일 패턴 강제.

## 롤백 정보
- 롤백 커밋: `git revert a20853a`
- 영향 범위: 예산 행 UI (FE only), monthly budget 도메인/BE 무영향
