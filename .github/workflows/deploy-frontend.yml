# DEPRECATED: GitHub Pages deployment replaced by NAS deployment (deploy-nas.yml)
# Kept for reference. Do not enable.

name: "[DEPRECATED] Deploy Frontend to GitHub Pages"

on:
  workflow_dispatch:  # Manual only, no auto trigger

jobs:
  deploy:
    if: false  # Disabled
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deprecated - use deploy-nas.yml"
