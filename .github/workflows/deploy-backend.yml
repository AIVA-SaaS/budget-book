# DEPRECATED: Render deployment replaced by NAS deployment (deploy-nas.yml)
# Kept for reference. Do not enable.

name: "[DEPRECATED] Deploy Backend to Render"

on:
  workflow_dispatch:  # Manual only, no auto trigger

jobs:
  deploy:
    if: false  # Disabled
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deprecated - use deploy-nas.yml"
